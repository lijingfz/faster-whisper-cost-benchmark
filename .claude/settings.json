{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.py) python3 -m py_compile \"$f\" || exit 2 ;; esac; }",
            "timeout": 15,
            "statusMessage": "Python 语法检查中..."
          }
        ]
      }
    ]
  }
}
